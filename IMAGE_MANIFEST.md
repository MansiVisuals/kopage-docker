# Image manifest

Generated automatically by `.github/workflows/build-and-push.yml`.
Describes the contents of the most recently published `crypt010/kopage:latest`.

This placeholder is replaced on the next build. Until then the published image
predates manifest tracking and its package versions were never recorded.
